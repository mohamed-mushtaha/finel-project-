<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.HotelBooking" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Brand colours wired into Material 3 colour roles -->
        <item name="colorPrimary">@color/emerald_500</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorPrimaryContainer">@color/emerald_100</item>
        <item name="colorOnPrimaryContainer">@color/emerald_900</item>
        <item name="colorSecondary">@color/sand_400</item>
        <item name="colorOnSecondary">@color/slate_900</item>
        <item name="colorSecondaryContainer">@color/sand_200</item>
        <item name="colorError">@color/coral_600</item>
        <item name="colorSurface">@color/slate_50</item>
        <item name="colorOnSurface">@color/slate_900</item>
        <item name="colorOutline">@color/slate_200</item>

        <!-- Status / nav bars — transparent, insets handled in code -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Typography -->
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- Splash: solid brand colour, no action bar -->
    <style name="Theme.HotelBooking.Splash" parent="Theme.HotelBooking">
        <item name="android:windowBackground">@color/splash_bg</item>
        <item name="android:statusBarColor">@color/splash_bg</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:navigationBarColor">@color/splash_bg</item>
    </style>

    <!-- Shared card / surface style -->
    <style name="Widget.HotelBooking.CardView" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">14dp</item>
        <item name="cardElevation">2dp</item>
    </style>

    <!-- Buttons -->
    <style name="Widget.HotelBooking.Button" parent="Widget.Material3.Button">
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <style name="Widget.HotelBooking.Button.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <!-- Text fields -->
    <style name="Widget.HotelBooking.TextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusTopStart">12dp</item>
        <item name="boxCornerRadiusTopEnd">12dp</item>
        <item name="boxCornerRadiusBottomStart">12dp</item>
        <item name="boxCornerRadiusBottomEnd">12dp</item>
    </style>

    <!-- Property card: round top corners only -->
    <style name="ShapeAppearance.PropertyCardImageTop" parent="">
        <item name="cornerFamilyTopLeft">rounded</item>
        <item name="cornerFamilyTopRight">rounded</item>
        <item name="cornerFamilyBottomLeft">cut</item>
        <item name="cornerFamilyBottomRight">cut</item>
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeTopRight">16dp</item>
    </style>

    <!-- Room card: round left corners only -->
    <style name="ShapeAppearance.RoomCardImageStart" parent="">
        <item name="cornerFamilyTopLeft">rounded</item>
        <item name="cornerFamilyBottomLeft">rounded</item>
        <item name="cornerFamilyTopRight">cut</item>
        <item name="cornerFamilyBottomRight">cut</item>
        <item name="cornerSizeTopLeft">12dp</item>
        <item name="cornerSizeBottomLeft">12dp</item>
    </style>
</resources>
